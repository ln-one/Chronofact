{
  "name": "chronofact",
  "version": "0.1.0",
  "private": true,
  "description": "Chronofact integration shell and blockchain course demo tooling.",
  "scripts": {
    "chain:ganache": "npx --yes ganache@7.9.2 --server.host 127.0.0.1 --server.port 7545 --chain.chainId 1337 --wallet.deterministic",
    "contracts:compile": "node scripts/compile_contract.js",
    "contracts:deploy:ganache": "node scripts/deploy_contract.js",
    "contracts:sample": "node scripts/register_sample_version.js",
    "test:api": "npm --prefix services/chronofact-api test",
    "test:agent": "npm --prefix services/chronofact-agent test",
    "test:ai": "cd services/ai-explanation && python3 -m unittest discover -s tests",
    "test:frontend": "npm --prefix services/frontend-demo run build",
    "test:integration": "node scripts/integration_smoke.mjs",
    "check:phase-one": "npm run test:api && npm run test:agent && npm run test:ai && npm run contracts:compile && npm run test:frontend && npm run test:integration"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ethers": "^6.13.5",
    "solc": "^0.8.28"
  },
  "engines": {
    "node": ">=20"
  }
}
